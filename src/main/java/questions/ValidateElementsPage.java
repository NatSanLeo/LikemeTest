/*
package questions;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

import static interactions.AcctionsAboutPage.getDriver;
import static org.junit.jupiter.api.Assertions.*;


import static org.asynchttpclient.util.Assertions.assertNotNull;

public class ValidateElementsPage {


    public void titlePageIsOK(){
        WebElement titlePage = getDriver().findElement(By.xpath("/html/body/div[1]/div/div/div[1]/div[2]/span"));
        assertNotNull(titlePage,"Resultado debe ser notNull");
        assertEquals("Products",titlePage.getText());
    }


    public void cardIsPresent(){
        WebElement card = getDriver().findElement(By.xpath("/html/body/div[1]/div/div/div[1]/div[1]/div[3]/a"));
        assertNotNull(card,"Resultado debe ser notNull");
        assertTrue(card.isDisplayed());
    }

    public void menuIsPresent(){
        WebElement menuIsPresent = getDriver().findElement(By.xpath("/html/body/div[1]/div/div/div[1]/div[1]/div[1]/div/div[1]/div/button"));
        assertNotNull(menuIsPresent,"Resultado debe ser notNull");
        assertTrue(menuIsPresent.isDisplayed());
    }
}
*/
